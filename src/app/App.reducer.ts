// import * as updaters from 'actions/updaters'

const initialState = {}

export default (state: any = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state
  }
}
