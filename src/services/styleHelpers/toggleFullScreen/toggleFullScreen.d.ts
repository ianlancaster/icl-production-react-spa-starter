/**
 * Pass in an html element (usually e.target or something like that)
 * to call a cross-browser fullScreen request on that element.
 */
export default function toggleFullScreen(element: HTMLElement): void
